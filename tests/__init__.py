"""Test package.

Marked as a package (rather than a loose directory) so shared helpers such as
`tests.conftest.NetworkAccessError` can be imported by name from any test
module without path games.
"""
